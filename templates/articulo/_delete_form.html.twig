<form method="post" action="{{ path('articulo_delete', {'id': articulo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ articulo.id) }}">
    <button class="btn bg-danger text-light mx-1 my-1">Eliminar</button>
</form>
